{
  "name": "bhunex-gis-converter",
  "version": "1.7.0",
  "private": true,
  "description": "BhuNex GIS Converter — GIS / Geomatics / Survey / CAD / Mining conversion suite as a Chrome MV3 extension.",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vite build --watch --mode development",
    "build": "vite build && node scripts/write-root-manifest.mjs",
    "package": "node scripts/package-extension.mjs && node scripts/verify-package.mjs",
    "docs": "node --experimental-strip-types scripts/format-matrix.mjs",
    "docs:check": "node --experimental-strip-types scripts/format-matrix.mjs --check",
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "offline:check": "node scripts/assert-offline.mjs",
    "verify": "npm run lint && npm run test && npm run docs:check && npm run build:store && npm run build:check && npm run offline:check && npm run store:check && npm run package",
    "build:store": "STORE_BUILD=1 vite build && node scripts/write-root-manifest.mjs",
    "store:check": "node scripts/assert-store-ready.mjs",
    "store:package": "npm run build:store && node scripts/assert-store-ready.mjs && node scripts/package-extension.mjs && node scripts/verify-package.mjs",
    "assets": "node scripts/make-store-assets.mjs",
    "package:check": "node scripts/verify-package.mjs",
    "bench": "vitest bench --run",
    "build:check": "node scripts/assert-build-committed.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "@types/node": "^22.10.2",
    "typescript": "~5.7.2",
    "vite": "^6.0.7",
    "vitest": "^2.1.9"
  },
  "author": "Md Salim Ansari"
}
